export interface SlideItem {
  id: string;
  imageSizes: string[];
  alt: string;
}

export interface OfferCardItems {
  id: string;
  label: string;
  body: string;
  icon: string;
}

export interface EbookCardItems {
  id: string;
  author: string;
  bookImage: string;
  bookTitle: string;
  price: string;
}
